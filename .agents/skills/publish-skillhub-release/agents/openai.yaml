interface:
  display_name: "Publish SkillHub Release"
  short_description: "Prepare, publish, and verify a SkillHub release"
  default_prompt: "Use $publish-skillhub-release to prepare, publish, and verify the next SkillHub server release."
